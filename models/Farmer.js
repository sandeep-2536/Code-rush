// Farmer model placeholder

// When ready, convert to a Mongoose schema or ORM model.

class Farmer {
  constructor({ id, name, email }) {
    this.id = id;
    this.name = name;
    this.email = email;
  }
}

module.exports = Farmer;
