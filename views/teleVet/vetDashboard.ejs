<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vet Dashboard - Incoming Calls</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gray-900">
    <%- include('../layouts/header') %>

    <div class="min-h-screen bg-gradient-to-br from-gray-900 via-purple-900 to-teal-900 p-6">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-4xl font-bold text-white mb-2">Vet Dashboard</h1>
            <p class="text-purple-300 mb-8">Dr. <%= vet.name %></p>

            <!-- Incoming Calls Section (simple placeholder) -->
            <div id="incomingCalls" class="space-y-4 mb-12">
                <div class="bg-white/10 backdrop-blur-md rounded-2xl p-8 border border-white/20 text-center">
                    <svg class="w-12 h-12 mx-auto mb-3 text-purple-400 animate-pulse" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M2 9a1 1 0 011-1h2.196a1 1 0 01.961.725l.213 1.005a7.001 7.001 0 006.658 0l.213-1.005a1 1 0 01.961-.725H15a1 1 0 011 1v.05A7.002 7.002 0 1 1 10 2a1 1 0 01-1 1H5a1 1 0 01-1-1v-.05A7.002 7.002 0 002 9v.05z" clip-rule="evenodd"/>
                    </svg>
                    <p class="text-purple-300 text-lg font-semibold">Waiting for incoming calls...</p>
                </div>
            </div>

            <!-- Vet Info Card -->
            <div class="bg-white/10 backdrop-blur-md rounded-2xl p-6 border border-white/20">
                <h2 class="text-xl font-bold text-white mb-4">Your Profile</h2>
                <div class="flex items-center gap-4">
                    <div class="w-20 h-20 rounded-full bg-purple-700 flex items-center justify-center text-white text-lg font-bold">
                        <%= vet.name ? vet.name.charAt(0).toUpperCase() : 'D' %>
                    </div>
                    <div>
                        <p class="text-white font-semibold">Dr. <%= vet.name %></p>
                        <p class="text-sm text-purple-300">Available for tele-consultations</p>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <%- include('../layouts/footer') %>

    <script src="/js/teleVetVetDashboard.js"></script>
</body>
</html>