<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Schemes - Admin Panel</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gradient-to-br from-green-50 via-white to-yellow-50 min-h-screen">

    <!-- Admin Navigation Bar -->
    <nav class="bg-gradient-to-r from-green-600 to-green-700 shadow-lg sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center gap-6">
                    <a href="/admin/dashboard" class="flex items-center gap-3 hover:opacity-80 transition">
                        <div class="bg-white/20 p-2 rounded-lg">
                            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z"/>
                            </svg>
                        </div>
                        <div>
                            <h1 class="text-white text-xl font-bold">Admin Panel</h1>
                            <p class="text-green-100 text-sm">Pending Schemes Review</p>
                        </div>
                    </a>
                </div>
                <div class="flex items-center gap-3">
                    <a href="/admin/schemes" class="bg-white/20 hover:bg-white/30 text-white px-4 py-2 rounded-lg font-semibold transition">
                        All Schemes
                    </a>
                    <a href="/admin/manual-fetch" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold transition">
                        🔄 Check Now
                    </a>
                    <a href="/admin/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg font-semibold transition">
                        Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 py-8">
        
        <!-- Header -->
        <div class="mb-8">
            <h2 class="text-3xl font-bold text-gray-800 mb-2">🤖 AI-Detected New Schemes</h2>
            <p class="text-gray-600">Review and approve schemes found by Gemini AI</p>
        </div>

        <!-- Success Message -->
        <% if (success) { %>
        <div class="mb-6 bg-green-50 border-l-4 border-green-500 p-4 rounded-lg">
            <p class="text-green-700 font-semibold"><%= success %></p>
        </div>
        <% } %>

        <!-- Error Message -->
        <% if (error) { %>
        <div class="mb-6 bg-red-50 border-l-4 border-red-500 p-4 rounded-lg">
            <p class="text-red-700 font-semibold"><%= error %></p>
        </div>
        <% } %>

        <!-- Info Box -->
        <div class="mb-8 bg-blue-50 border-l-4 border-blue-500 p-4 rounded-lg">
            <div class="flex items-start">
                <svg class="w-6 h-6 text-blue-500 mr-3 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                </svg>
                <div>
                    <p class="text-blue-700 font-semibold mb-1">How it works</p>
                    <ul class="text-blue-600 text-sm space-y-1">
                        <li>• Gemini AI checks for new schemes every 12 hours automatically</li>
                        <li>• Review each scheme for accuracy before approving</li>
                        <li>• Approved schemes will notify farmers via SMS</li>
                        <li>• Click "Check Now" to manually trigger a search</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Pending Schemes -->
        <% if (pendingSchemes && pendingSchemes.length > 0) { %>
        <div class="space-y-6">
            <% pendingSchemes.forEach(scheme => { %>
            <div class="bg-white rounded-2xl shadow-lg p-6 border-l-4 border-yellow-500">
                <div class="flex items-start justify-between mb-4">
                    <div class="flex-1">
                        <div class="flex items-center gap-3 mb-3">
                            <h3 class="text-2xl font-bold text-gray-800"><%= scheme.name %></h3>
                            <span class="px-3 py-1 bg-yellow-100 text-yellow-800 rounded-full text-xs font-semibold uppercase">
                                <%= scheme.confidence %> confidence
                            </span>
                        </div>
                        <p class="text-gray-600 mb-4"><%= scheme.desc %></p>
                        <div class="flex gap-3 flex-wrap mb-3">
                            <span class="px-3 py-1 bg-green-100 text-green-800 rounded-full text-sm font-semibold">
                                <%= scheme.state %>
                            </span>
                            <span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-semibold">
                                <%= scheme.type %>
                            </span>
                        </div>
                        <p class="text-sm text-gray-500">
                            <strong>Source:</strong> <%= scheme.source %><br>
                            <strong>Detected:</strong> <%= new Date(scheme.createdAt).toLocaleString() %>
                        </p>
                    </div>
                </div>
                
                <div class="flex gap-3 pt-4 border-t border-gray-200">
                    <a href="/admin/pending-schemes/approve/<%= scheme._id %>" 
                       class="flex-1 bg-gradient-to-r from-green-600 to-green-700 text-white py-3 px-6 rounded-xl font-bold hover:shadow-lg transition transform hover:scale-105 text-center">
                        ✅ Approve & Notify Farmers
                    </a>
                    <a href="/admin/pending-schemes/reject/<%= scheme._id %>" 
                       class="bg-red-500 hover:bg-red-600 text-white py-3 px-6 rounded-xl font-bold transition text-center"
                       onclick="return confirm('Are you sure you want to reject this scheme?')">
                        ❌ Reject
                    </a>
                </div>
            </div>
            <% }) %>
        </div>
        <% } else { %>
        <div class="bg-white rounded-2xl shadow-lg p-12 text-center">
            <svg class="w-24 h-24 mx-auto text-gray-300 mb-4" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
            </svg>
            <p class="text-2xl font-bold text-gray-400 mb-2">No pending schemes</p>
            <p class="text-gray-500 mb-6">Gemini AI hasn't detected any new schemes yet</p>
            <a href="/admin/manual-fetch" class="inline-block bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-xl font-semibold transition">
                🔄 Check for New Schemes Now
            </a>
        </div>
        <% } %>

    </main>

</body>
</html>