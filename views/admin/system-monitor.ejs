<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Monitor - Admin Panel</title>
    <link rel="stylesheet" href="/css/output.css">
    <style>
        .pulse { animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite; }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: .5; }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-green-50 via-white to-yellow-50 min-h-screen">

    <!-- Admin Navigation Bar -->
    <nav class="bg-gradient-to-r from-green-600 to-green-700 shadow-lg sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center gap-6">
                    <a href="/admin/dashboard" class="flex items-center gap-3 hover:opacity-80 transition">
                        <div class="bg-white/20 p-2 rounded-lg">
                            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z"/>
                            </svg>
                        </div>
                        <div>
                            <h1 class="text-white text-xl font-bold">Admin Panel</h1>
                            <p class="text-green-100 text-sm">System Monitor</p>
                        </div>
                    </a>
                </div>
                <div class="flex items-center gap-3">
                    <a href="/admin/dashboard" class="bg-white/20 hover:bg-white/30 text-white px-4 py-2 rounded-lg font-semibold transition">
                        Dashboard
                    </a>
                    <a href="/admin/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg font-semibold transition">
                        Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 py-8">
        
        <div class="mb-8">
            <h2 class="text-3xl font-bold text-gray-800 mb-2">🔍 System Monitor</h2>
            <p class="text-gray-600">Real-time status of Gemini AI integration</p>
        </div>

        <!-- System Status Cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            
            <!-- Gemini Status -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-l-4 <%= geminiConfigured ? 'border-green-500' : 'border-red-500' %>">
                <div class="flex items-center justify-between mb-3">
                    <div class="<%= geminiConfigured ? 'bg-green-100' : 'bg-red-100' %> p-3 rounded-xl">
                        <svg class="w-8 h-8 <%= geminiConfigured ? 'text-green-600' : 'text-red-600' %>" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M11.3 1.046A1 1 0 0112 2v5h4a1 1 0 01.82 1.573l-7 10A1 1 0 018 18v-5H4a1 1 0 01-.82-1.573l7-10a1 1 0 011.12-.38z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <% if (geminiConfigured) { %>
                        <div class="w-3 h-3 bg-green-500 rounded-full pulse"></div>
                    <% } %>
                </div>
                <h3 class="text-gray-500 text-sm font-medium">Gemini AI</h3>
                <p class="text-2xl font-bold <%= geminiConfigured ? 'text-green-600' : 'text-red-600' %>">
                    <%= geminiConfigured ? 'Active' : 'Not Configured' %>
                </p>
            </div>

            <!-- Twilio Status -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-l-4 <%= twilioConfigured ? 'border-blue-500' : 'border-yellow-500' %>">
                <div class="flex items-center justify-between mb-3">
                    <div class="<%= twilioConfigured ? 'bg-blue-100' : 'bg-yellow-100' %> p-3 rounded-xl">
                        <svg class="w-8 h-8 <%= twilioConfigured ? 'text-blue-600' : 'text-yellow-600' %>" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                            <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
                        </svg>
                    </div>
                    <% if (twilioConfigured) { %>
                        <div class="w-3 h-3 bg-blue-500 rounded-full pulse"></div>
                    <% } %>
                </div>
                <h3 class="text-gray-500 text-sm font-medium">SMS Service</h3>
                <p class="text-2xl font-bold <%= twilioConfigured ? 'text-blue-600' : 'text-yellow-600' %>">
                    <%= twilioConfigured ? 'Active' : 'Optional' %>
                </p>
            </div>

            <!-- Next Run -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-l-4 border-purple-500">
                <div class="bg-purple-100 p-3 w-fit rounded-xl mb-3">
                    <svg class="w-8 h-8 text-purple-600" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                    </svg>
                </div>
                <h3 class="text-gray-500 text-sm font-medium">Next Auto Check</h3>
                <p class="text-xl font-bold text-purple-600"><%= timeUntilNext %></p>
                <p class="text-xs text-gray-500 mt-1"><%= nextRun %></p>
            </div>

            <!-- Pending Count -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-l-4 border-yellow-500">
                <div class="bg-yellow-100 p-3 w-fit rounded-xl mb-3">
                    <svg class="w-8 h-8 text-yellow-600" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M9 2a1 1 0 000 2h2a1 1 0 100-2H9z"/>
                        <path fill-rule="evenodd" d="M4 5a2 2 0 012-2 3 3 0 003 3h2a3 3 0 003-3 2 2 0 012 2v11a2 2 0 01-2 2H6a2 2 0 01-2-2V5zm3 4a1 1 0 000 2h.01a1 1 0 100-2H7zm3 0a1 1 0 000 2h3a1 1 0 100-2h-3z" clip-rule="evenodd"/>
                    </svg>
                </div>
                <h3 class="text-gray-500 text-sm font-medium">Pending Review</h3>
                <p class="text-3xl font-bold text-yellow-600"><%= stats.pendingSchemes || 0 %></p>
            </div>

        </div>

        <!-- Statistics Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            
            <div class="bg-white rounded-2xl shadow-lg p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-4">📊 Overall Stats</h3>
                <div class="space-y-3">
                    <div class="flex justify-between">
                        <span class="text-gray-600">Total Approved Schemes:</span>
                        <span class="font-bold text-green-600"><%= stats.totalApprovedSchemes || 0 %></span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">Total AI Detections:</span>
                        <span class="font-bold text-gray-800"><%= stats.totalPendingEver || 0 %></span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">Pending Review:</span>
                        <span class="font-bold text-yellow-600"><%= stats.pendingSchemes || 0 %></span>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-2xl shadow-lg p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-4">✅ Approval Rate</h3>
                <div class="space-y-3">
                    <div class="flex justify-between">
                        <span class="text-gray-600">Approved:</span>
                        <span class="font-bold text-green-600"><%= stats.totalApproved || 0 %></span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">Rejected:</span>
                        <span class="font-bold text-red-600"><%= stats.totalRejected || 0 %></span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">Approval Rate:</span>
                        <span class="font-bold text-blue-600">
                            <% const total = (stats.totalApproved || 0) + (stats.totalRejected || 0); %>
                            <%= total > 0 ? Math.round((stats.totalApproved / total) * 100) : 0 %>%
                        </span>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-2xl shadow-lg p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-4">📅 Today's Activity</h3>
                <div class="space-y-3">
                    <div class="flex justify-between">
                        <span class="text-gray-600">Approved Today:</span>
                        <span class="font-bold text-green-600"><%= stats.approvedToday || 0 %></span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">Rejected Today:</span>
                        <span class="font-bold text-red-600"><%= stats.rejectedToday || 0 %></span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">Total Actions:</span>
                        <span class="font-bold text-gray-800"><%= (stats.approvedToday || 0) + (stats.rejectedToday || 0) %></span>
                    </div>
                </div>
            </div>

        </div>

        <!-- Recent Activity -->
        <div class="bg-white rounded-2xl shadow-lg p-8 mb-8">
            <div class="flex items-center justify-between mb-6">
                <h3 class="text-2xl font-bold text-gray-800">📋 Recent AI Detections</h3>
                <a href="/admin/pending-schemes" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-semibold transition">
                    View All Pending
                </a>
            </div>

            <% if (recentPending && recentPending.length > 0) { %>
                <div class="space-y-3">
                    <% recentPending.forEach(scheme => { %>
                        <div class="flex items-center justify-between p-4 border border-gray-200 rounded-xl hover:bg-gray-50 transition">
                            <div class="flex-1">
                                <h4 class="font-bold text-gray-800"><%= scheme.name %></h4>
                                <div class="flex gap-2 mt-2">
                                    <span class="px-2 py-1 bg-green-100 text-green-800 rounded text-xs"><%= scheme.state %></span>
                                    <span class="px-2 py-1 bg-blue-100 text-blue-800 rounded text-xs"><%= scheme.type %></span>
                                    <span class="px-2 py-1 bg-purple-100 text-purple-800 rounded text-xs"><%= scheme.confidence %> confidence</span>
                                </div>
                            </div>
                            <div class="text-right">
                                <% if (scheme.status === 'pending') { %>
                                    <span class="px-3 py-1 bg-yellow-100 text-yellow-800 rounded-full text-sm font-semibold">Pending</span>
                                <% } else if (scheme.status === 'approved') { %>
                                    <span class="px-3 py-1 bg-green-100 text-green-800 rounded-full text-sm font-semibold">Approved</span>
                                <% } else { %>
                                    <span class="px-3 py-1 bg-red-100 text-red-800 rounded-full text-sm font-semibold">Rejected</span>
                                <% } %>
                                <p class="text-xs text-gray-500 mt-1"><%= new Date(scheme.createdAt).toLocaleDateString() %></p>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="text-center py-12">
                    <svg class="w-16 h-16 mx-auto text-gray-300 mb-4" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                    </svg>
                    <p class="text-xl font-bold text-gray-400">No recent activity</p>
                    <p class="text-gray-500 mt-2">Gemini AI hasn't detected any schemes yet</p>
                </div>
            <% } %>
        </div>

        <!-- Quick Actions -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <a href="/admin/manual-fetch" class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl p-6 text-white shadow-lg hover:shadow-xl transition">
                <div class="flex items-center gap-4">
                    <div class="bg-white/20 p-4 rounded-xl">
                        <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M4 2a1 1 0 011 1v2.101a7.002 7.002 0 0111.601 2.566 1 1 0 11-1.885.666A5.002 5.002 0 005.999 7H9a1 1 0 010 2H4a1 1 0 01-1-1V3a1 1 0 011-1zm.008 9.057a1 1 0 011.276.61A5.002 5.002 0 0014.001 13H11a1 1 0 110-2h5a1 1 0 011 1v5a1 1 0 11-2 0v-2.101a7.002 7.002 0 01-11.601-2.566 1 1 0 01.61-1.276z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div>
                        <h4 class="font-bold text-lg">Check Now</h4>
                        <p class="text-blue-100 text-sm">Trigger manual search</p>
                    </div>
                </div>
            </a>

            <a href="/admin/pending-schemes" class="bg-gradient-to-br from-yellow-500 to-yellow-600 rounded-2xl p-6 text-white shadow-lg hover:shadow-xl transition">
                <div class="flex items-center gap-4">
                    <div class="bg-white/20 p-4 rounded-xl">
                        <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M9 2a1 1 0 000 2h2a1 1 0 100-2H9z"/>
                            <path fill-rule="evenodd" d="M4 5a2 2 0 012-2 3 3 0 003 3h2a3 3 0 003-3 2 2 0 012 2v11a2 2 0 01-2 2H6a2 2 0 01-2-2V5zm3 4a1 1 0 000 2h.01a1 1 0 100-2H7zm3 0a1 1 0 000 2h3a1 1 0 100-2h-3z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div>
                        <h4 class="font-bold text-lg">Review Schemes</h4>
                        <p class="text-yellow-100 text-sm">Approve or reject pending</p>
                    </div>
                </div>
            </a>

            <a href="/admin/schemes" class="bg-gradient-to-br from-green-500 to-green-600 rounded-2xl p-6 text-white shadow-lg hover:shadow-xl transition">
                <div class="flex items-center gap-4">
                    <div class="bg-white/20 p-4 rounded-xl">
                        <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M6 2a2 2 0 00-2 2v12a2 2 0 002 2h8a2 2 0 002-2V7.414A2 2 0 0015.414 6L12 2.586A2 2 0 0010.586 2H6zm5 6a1 1 0 10-2 0v3.586l-1.293-1.293a1 1 0 10-1.414 1.414l3 3a1 1 0 001.414 0l3-3a1 1 0 00-1.414-1.414L11 11.586V8z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div>
                        <h4 class="font-bold text-lg">All Schemes</h4>
                        <p class="text-green-100 text-sm">View approved schemes</p>
                    </div>
                </div>
            </a>
        </div>

    </main>

</body>
</html>