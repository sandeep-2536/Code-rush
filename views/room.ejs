<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aarohi Farmer - Doctor Connect</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://unpkg.com/peerjs@1.4.7/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="/js/videocall.js" defer></script>

    <style>
        /* Custom grid for video */
        #video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1rem;
        }
        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 0.5rem;
            transform: scaleX(-1); /* Mirror effect */
        }
    </style>
</head>
<body class="bg-green-50 h-screen flex flex-col">

    <header class="bg-green-700 text-white p-4 shadow-md flex justify-between items-center" data-room-id="<%= roomId %>">
        <div class="flex items-center space-x-2">
            <i class="fa-solid fa-leaf text-yellow-300 text-xl"></i>
            <h1 class="text-xl font-bold">Aarohi Farmer Connect</h1>
        </div>
        <div class="bg-green-800 px-3 py-1 rounded text-sm">
            <span class="text-green-200">Session ID:</span> <span id="sessionIdDisplay"><%= roomId %></span>
        </div>
    </header>

    <main class="flex-grow p-6 overflow-y-auto">
        <div class="max-w-6xl mx-auto h-full flex flex-col justify-center">
            
            <div id="video-grid" class="w-full">
                </div>

            <div id="waiting-msg" class="text-center mt-10 text-gray-500">
                <p class="text-lg">Waiting for the Doctor to join...</p>
                <p class="text-sm">Share the URL above to connect.</p>
            </div>
        </div>
    </main>

    <footer class="bg-white p-4 shadow-lg border-t border-gray-200">
        <div class="flex justify-center space-x-6">
            <button class="p-4 rounded-full bg-gray-200 hover:bg-gray-300 text-gray-700 transition" title="Toggle Mic">
                <i class="fa-solid fa-microphone"></i>
            </button>
            <button class="p-4 rounded-full bg-gray-200 hover:bg-gray-300 text-gray-700 transition" title="Toggle Camera">
                <i class="fa-solid fa-video"></i>
            </button>
            <button class="p-4 rounded-full bg-red-600 hover:bg-red-700 text-white transition shadow-lg" title="End Call">
                <i class="fa-solid fa-phone-slash"></i>
            </button>
        </div>
    </footer>

    <!-- Config data element for videocall.js -->
    <div id="videoConfig" style="display:none" data-room-id="<%= roomId %>"></div>

    <script>
        // Extract roomId from data attribute
        window.ROOM_ID = document.getElementById('videoConfig').dataset.roomId || document.querySelector('header').dataset.roomId;
    </script>

</body>
</html>